/** @note These will change, they are just temporary for now */

const colors = {
  primary: "#007bff",
  secondary: "#20c997"
};

const sizes = {
  sm: "4px",
  md: "8px",
  lg: "12px",
  xl: "16px"
};

export default { colors, sizes };
